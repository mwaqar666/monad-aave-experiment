{
  "name": "@packages/db",
  "version": "0.0.1",
  "description": "Shared Drizzle schema for static market metadata tables (owned by seeder)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "bun run tools/clean.ts",
    "build": "bun clean && bun run tools/build.ts",
    "schema:generate": "bun --bun drizzle-kit generate"
  },
  "peerDependencies": {
    "@packages/core": "workspace:^",
    "drizzle-orm": "catalog:dependencies"
  },
  "devDependencies": {
    "@types/bun": "catalog:devDependencies",
    "@types/node": "catalog:devDependencies",
    "drizzle-kit": "catalog:devDependencies",
    "typescript": "catalog:devDependencies"
  }
}
